server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Andriizachepilo/azure-webclinic/tree/main/config
          default-label: main

        native:
          searchLocations: file:///${GIT_REPO}

