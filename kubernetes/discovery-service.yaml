apiVersion: v1
kind: Service
metadata:
  name: discovery-server
spec:
  selector:
    app: discovery-server
  ports:
    - protocol: TCP
      port: 8761
      targetPort: 8761
  type: ClusterIP

  # admin 9090
  # api-gateway 8080
  # conf 8888
  # customer 8806
  # discovery-server 8761
  # vets  8807
  # visits 8808
